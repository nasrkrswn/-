# البيانات المطلوبة لاستكمال الإعداد

## حالة Supabase الحالية

- تم استلام رابط المشروع والمفتاح publishable ومفتاح anon القديم ومفتاح service_role وإضافتها إلى `.env.local`.
- تم فحص الاتصال بالمشروع ونجح الوصول إلى Supabase، ومفتاح الخدمة يعمل مع Admin Auth.
- الفحص الأحدث أظهر أن الجداول الأساسية غير موجودة في Schema Cache، مثل `public.profiles` و `public.departments` و `public.attendance_records`.
- تم إصلاح ملف `supabase/migrations/001_initial_schema.sql` ليكون أكثر أمانًا عند التنفيذ، وتمت إضافة أمر `npm run db:setup` لتطبيق قاعدة البيانات آليًا.
- الخطوة المطلوبة الآن: إضافة `SUPABASE_ACCESS_TOKEN` إلى `.env.local` ثم تشغيل `npm run db:setup`، أو تنفيذ ملف `supabase/migrations/001_initial_schema.sql` يدويًا داخل Supabase SQL Editor ثم تنفيذ `supabase/seed.sql`.

## قرارات Supabase المتبقية

- مفتاح `SUPABASE_SERVICE_ROLE_KEY` موجود محليًا فقط ولا يوضع في الواجهة.
- نحتاج `SUPABASE_ACCESS_TOKEN` مرة واحدة فقط لتطبيق SQL آليًا عبر Supabase Management API.
- هل تريد السماح بتسجيل حسابات جديدة من صفحة الدخول، أم يكون إنشاء الحسابات من الإدارة فقط؟

## بيانات الشركة

- اللغة المعتمدة للواجهة: عربي.
- تم تجهيز متغير `NEXT_PUBLIC_COMPANY_NAME` لاسم الشركة الظاهر في الواجهة. القيمة الحالية: `حضورك`.
- تم تجهيز متغير `NEXT_PUBLIC_BRAND_PRIMARY` للون الأساسي. القيمة الحالية: `#059669`.
- تم تجهيز متغير `NEXT_PUBLIC_COMPANY_LOGO_URL` للشعار إن وجد.
- أرسل اسم الشركة واللون أو رابط الشعار لو تريد استبدال القيم الحالية.

## مواقع العمل

لكل موقع:

- اسم الموقع.
- خط العرض latitude.
- خط الطول longitude.
- النطاق المسموح بالمتر radius_meters.
- هل الموقع نشط أم لا.

## الأقسام والموظفون

لكل قسم:

- اسم القسم.
- المدير أو المشرف المسؤول عنه.

لكل موظف:

- الاسم الكامل.
- البريد الإلكتروني.
- الدور: employee أو supervisor أو manager أو admin.
- القسم.
- حالة الحساب: نشط أو موقوف.

## الورديات

لكل وردية:

- اسم الوردية.
- وقت بداية الدوام.
- وقت نهاية الدوام.
- مدة السماح للتأخير بالدقائق.
- الأقسام أو الموظفون المرتبطون بها، إذا كان هناك أكثر من نظام دوام.

## قواعد الحضور

- يحسب التأخير بعد فترة السماح مباشرة: نعم.
- لا يسمح بتسجيل الانصراف خارج الموقع.
- يسمح بتسجيل حضور أكثر من مرة يوميًا: أكثر من فترة.
- يحتاج الموظف إلى موافقة عند تصحيح الحضور.
- توجد أيام عطلات أسبوعية ورسمية يجب تجاهلها في التقارير.

## التقارير والتنبيهات

- شكل التقرير المطلوب للشهرية واليومية.
- بيانات التوقيع أو الختم في PDF إن كانت مطلوبة.
- قنوات التنبيه المطلوبة: داخل النظام، بريد إلكتروني، واتساب، أو غير ذلك.

## النشر والتكاملات

- رابط مستودع GitHub.
- حساب أو مشروع Vercel.
- نطاق الموقع إن وجد.
- `NEXT_PUBLIC_FACEBOOK_PIXEL_ID` إن كنت تريد تفعيل Meta Pixel.
